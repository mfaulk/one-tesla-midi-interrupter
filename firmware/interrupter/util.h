/*
 * Utility functions
 */
 
#ifndef __UTILS_H

#include "system.h"
#include "constants.h"

int getOnTime(long frequency);
long getFrequency(byte pitch);
void playNotes();

#define __UTILS_H
#endif
